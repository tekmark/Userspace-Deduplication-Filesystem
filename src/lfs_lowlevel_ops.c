#include "lfs_lowlevel_ops.h"

static struct fuse_lowlevel_ops lfs_ll_opers = {
    /*.lookup         = lfs_ll_lookup,
    .getattr        = lfs_ll_getattr,
    .readdir        = lfs_ll_readdir,
    .open           = lfs_ll_open,
    .read           = lfs_ll_read,*/
};
